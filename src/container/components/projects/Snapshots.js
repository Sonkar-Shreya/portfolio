import React from "react";

const Snapshots = ({ color = 'gray', projectDetails={} }) => {
    return (
        <div>

            {/* 
                snapshots with one line description

                parallax ho to better rahega
            
            */}
            this contains snapshot
        </div>
    )
}

export default Snapshots